class Node {
  constructor() { 
  }
}

class MySimpleLinkedList {
  constructor() {
  }

  //método que agrega en la cola de la linkedlist
  append(value) {}

  //método que agrega un valor al primer elemento de la lista
  prepend(value) {}

  //agrega un elemento en el indice indicado
  insert(index, value) {}

  //obtenemos el nodo previo al consultado en el index
  getTheIndex(index) {}
}
