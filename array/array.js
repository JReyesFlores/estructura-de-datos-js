class MyArray {
  //Definimos las propiedades por almacenar
  constructor() {}

  //método para agregar items al array
  push(item) {}

  //método para eliminar el último elemento de un array
  pop() {}

  //Agrega un elemento al inicio del array
  unshift(item) {}

  //Elimina y retorna el primer elemento
  shift() {}

  //Agrega un elemento en una parte del array
  splice(pos, item) {}

  //método que retorna el valor del indice indicado
  get(index) {}

  //elimina el elemento de la posición indicada.
  delete(index) {}

  //reemplazará el valor del indice indicado con el valor del siguiente elemento 
  //y así susecivamente.
  shiftIndex(index) {}
}
